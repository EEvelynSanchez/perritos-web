// Centralized list of protected routes for the middleware matcher
// Editing this file will update the middleware matcher configuration

export const protectedRoutes = [
  "/dogs/:path*",
  "/create/:path*",
  "/profile/:path*",
  "/account/:path*"
]
